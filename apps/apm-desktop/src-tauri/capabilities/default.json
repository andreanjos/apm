{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app capability for the apm desktop shell",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
